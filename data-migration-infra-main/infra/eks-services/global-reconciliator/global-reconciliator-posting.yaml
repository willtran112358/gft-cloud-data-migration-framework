apiVersion: batch/v1
kind: Job
metadata:
  name: global-reconciliator-posting
  namespace: dataloader
spec:
  template:
    spec:
      serviceAccountName: data-migration-services
      imagePullSecrets:
      - name: regcredb
      containers:
      - name: global-reconciliator-posting
        image: global-reconciliator-api
        imagePullPolicy: Always
        env:
          - name: ENTITY
            value: "posting"        
      restartPolicy: Never
  backoffLimit: 0     